key: 939bf3d3bd1e80c8
request:
  model: claude-sonnet-4-20250514
  system: You are analyzing the codebase "interactive-reviewer". Summarize this
    module concisely.
  messages:
    - role: user
      content: >-
        Write a substantive summary of this module that a developer would
        actually find useful. The summary will be spoken aloud as part of a
        guided code-review tour.


        REQUIRED — your summary must include all three:

        1. The central concept the module owns (one sentence — the thing that,
        if you understood nothing else, would orient you).

        2. A non-obvious detail or surprise: a constraint, an edge case, a "but
        actually" that a reader exploring just the directory listing would miss.

        3. A specific file (by name) that does the heaviest lifting and what it
        owns. Use the actual file name from the list below.


        Forbidden:

        - No filler ("This module handles…", "Various utilities for…").

        - No marketing language.

        - Don't enumerate every file. Three at most, only if each does something
        meaningfully different.

        - Don't end with "…and helpers" or "…and tests" — be specific or omit.


        Length: 3–4 sentences total. Conversational tone (it's spoken, not
        read).


        Module: test



        Files in this module (with one-line summaries):

        - test/e2e/explore-mode-no-blank-screen.spec.ts (test)

        - test/e2e/lobby.spec.ts (test)

        - test/e2e/session-entry.spec.ts (test)

        - test/fixtures/create-hermes-fixture.sh (test)

        - test/fixtures/create-medium-repo.sh (test)

        - test/fixtures/create-small-repo.sh (test)

        - test/fixtures/create-small-walkthrough.sh (test)

        - test/frontend/deep-link-handling.test.tsx (test)

        - test/frontend/lobby-renders.test.tsx (test)

        - test/frontend/no-voice-barge-in.test.tsx (test)

        - test/frontend/orchestrator-no-self-interrupt.test.tsx (test)

        - test/frontend/pause-stops-mic.test.tsx (test)

        - test/frontend/push-to-talk.test.tsx (test)

        - test/frontend/room-renders-for-every-phase.test.tsx (test)

        - test/harness/cassettes-audit.ts (test)

        - test/harness/client.ts (test)

        - test/harness/index.ts (test)

        - test/harness/run-session.ts (test)

        - test/harness/server.ts (test)

        - test/integration/audio/voice-simulate.test.ts (test)
  maxTokens: 200
response:
  text: This module owns end-to-end testing of the interactive code review system,
    from voice simulation through complete user workflows. The surprising thing
    is that it includes shell scripts that generate entire fake repositories as
    test fixtures—not just mocking data structures, but actually creating Git
    repos with realistic commit histories. The `test/harness/run-session.ts`
    file does the heavy lifting by orchestrating full session simulations that
    can replay recorded interactions through "cassettes," letting you test
    complex voice-driven review flows without needing real speech recognition
    during CI.
  usage:
    inputTokens: 622
    outputTokens: 115
