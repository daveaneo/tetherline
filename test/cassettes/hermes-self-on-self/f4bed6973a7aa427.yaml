key: f4bed6973a7aa427
request:
  model: claude-sonnet-4-20250514
  system: You are analyzing the codebase "interactive-reviewer". Summarize this
    module concisely.
  messages:
    - role: user
      content: >-
        Write a substantive summary of this module that a developer would
        actually find useful. The summary will be spoken aloud as part of a
        guided code-review tour.


        REQUIRED — your summary must include all three:

        1. The central concept the module owns (one sentence — the thing that,
        if you understood nothing else, would orient you).

        2. A non-obvious detail or surprise: a constraint, an edge case, a "but
        actually" that a reader exploring just the directory listing would miss.

        3. A specific file (by name) that does the heaviest lifting and what it
        owns. Use the actual file name from the list below.


        Forbidden:

        - No filler ("This module handles…", "Various utilities for…").

        - No marketing language.

        - Don't enumerate every file. Three at most, only if each does something
        meaningfully different.

        - Don't end with "…and helpers" or "…and tests" — be specific or omit.


        Length: 3–4 sentences total. Conversational tone (it's spoken, not
        read).


        Module: backend



        Files in this module (with one-line summaries):

        - packages/backend/src/db/database.ts (other)

        - packages/backend/src/config.ts (config)

        - packages/backend/src/skills/registry.ts (other)

        - packages/backend/src/db/repositories/context-cache-repo.ts (other)

        - packages/backend/src/dev/trace.ts (other)

        - packages/backend/src/intelligence/llm/types.ts (type)

        - packages/backend/src/intelligence/client-interface.ts (other)

        - packages/backend/src/server.ts (entry)

        - packages/backend/src/cache/context-composer.ts (other)

        - packages/backend/src/intelligence/llm/canonicalize.ts (other)

        - packages/backend/src/session/manager.ts (other)

        - packages/backend/src/tts/provider.ts (other)

        - packages/backend/src/briefing/composer.ts (other)

        - packages/backend/src/briefing/tts-safety.ts (other)

        - packages/backend/src/cache/hash-utils.ts (utility)

        - packages/backend/src/cache/workspace-detector.ts (other)

        - packages/backend/src/db/repositories/briefing-repo.ts (other)

        - packages/backend/src/db/repositories/comprehension-repo.ts (other)

        - packages/backend/src/db/repositories/heatmap-repo.ts (other)

        - packages/backend/src/digest/generator.ts (other)
  maxTokens: 200
response:
  text: This backend orchestrates AI-powered code review sessions by composing
    contextual briefings from cached workspace analysis and delivering them
    through text-to-speech. The surprising constraint is that it heavily caches
    workspace context and analysis results to avoid expensive recomputation, but
    must detect when files change to invalidate those caches appropriately. The
    `briefing/composer.ts` file does the core work of synthesizing code context,
    AI analysis, and session state into the spoken briefings that guide
    developers through their code.
  usage:
    inputTokens: 636
    outputTokens: 108
